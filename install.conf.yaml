- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:

- create:
    - ~/downloads
    - ~/projects
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]
