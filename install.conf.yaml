- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.scripts:
    ~/.xinitrc:
    ~/.zprofile:
    ~/.zshrc:

- create:
    - ~/downloads
    - ~/.images
    - ~/projects
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]
